<UserControl x:Class="DialogueNodeEditor.UserControls.ShortcutRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="Root"
             Margin="0,3">
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Key chips — fixed width so descriptions align -->
            <ColumnDefinition Width="195"/>
            <!-- Description -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Key chip(s) — individual tokens split on '+' and rendered as chips -->
        <ItemsControl Grid.Column="0"
                      ItemsSource="{Binding KeyTokens, ElementName=Root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <!-- The token itself (key name or '+' separator) -->
                        <Border Margin="0,0,3,0"
                                VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Key chip look for normal tokens -->
                                    <Setter Property="Background"      Value="#2D2D30"/>
                                    <Setter Property="BorderBrush"     Value="#555555"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="CornerRadius"    Value="4"/>
                                    <Setter Property="Padding"         Value="7,2"/>
                                    <Style.Triggers>
                                        <!-- Separator '+' gets no chip styling -->
                                        <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                            <Setter Property="Background"      Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Padding"         Value="2,2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Label}"
                                       FontFamily="Consolas, Courier New"
                                       FontSize="12"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#E0E0E0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                <Setter Property="Foreground" Value="#666666"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Description -->
        <TextBlock Grid.Column="1"
                   Text="{Binding Description, ElementName=Root}"
                   Foreground="#AAAAAA"
                   FontSize="12"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
